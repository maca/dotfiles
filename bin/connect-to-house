#!/bin/sh

# addr=$($HOME/bin/house-addr)
# host=$(echo $addr | cut -d ':' -f1)
# port=$(echo $addr | cut -d ':' -f2)
# ssh -p $port $host -t 'LANG=en_US.UTF-8 exec tmux attach'

ssh root@192.168.15.2 -t 'TERM=xterm exec tmux attach || tmux new'
