#!/usr/bin/sh

xrandr | grep ' connected' | cut -d' ' -f1 | xargs -I '{}' xrandr --output '{}' --off
screen_count=$(xrandr | grep ' connected ' | wc -l)
sleep 2

if [[ $screen_count -gt 1 ]]
  then $HOME/.screenlayout/$(hostname).sh
  else xrandr --auto
fi

