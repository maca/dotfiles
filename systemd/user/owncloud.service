[Unit]
Description=Owncloud container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a owncloud
ExecStop=/usr/bin/docker stop -t 2 owncloud

[Install]
WantedBy=local.target
